        {
        "name": "{{ var "waitname" ?: "Wait" }}",
        "refId": "{{ var "refId" ?: "1" }}",
        "requisiteStageRefIds": [],
        "type": "wait",
        "waitTime": {{ var "waitTime" ?: 30 }}
        }